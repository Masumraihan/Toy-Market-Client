import React from 'react';
import useTitle from '../../Hooks/UseTitle';

const Blogs = () => {
    useTitle("Blogs")
    return (
        <div>
            <h1>blog page</h1>
        </div>
    );
};

export default Blogs;